# Deployments

Shared deployment artifacts should be stored in this directory.

Expected file after Sepolia deployment:

- `sepolia.json`

This file contains only public deployment metadata such as contract addresses, chain id, deployer address, and block number, so it is intended to be committed for the team.
